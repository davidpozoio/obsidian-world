The interpreters assign each expression to an action, so they don't need to compile the code, but execute it directly.
## Tree-walk interpreters
These tree-walk interpreters uses a syntax tree to execute the code and they execute it as it goes each node.
An argument is the current value that we passed to a function when we call it.
```java
someFunction(argument);
```
And a parameter is the variable that saves that `argument` in the body of the function.
```java
void someFunction(String parameter){
	//some logic
}
```